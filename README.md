# Babyfoot

## Requirements
- Docker
- Docker-Compose

## Build
```shell
docker-compose up
```
## Target Linux (Default)
Set target in the `.env` file
```env
TARGET=LINUX
```
## Target Windows
Set target in the `.env` file
```env
TARGET=WINDOWS
```