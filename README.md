# Babyfoot

## Requirements
- Docker
- Docker-Compose

## Target Linux (Default)
Set target in the `.env` file
```env
TARGET=LINUX
```
## Target Linux
Set target in the `.env` file
```env
TARGET=WINDOWS
```